"""banxe-trading-backend — network transport for ExchangePort + MarketDataPort.

SKELETON only (ADR-021, HANDOFF IL-188): REST/WS surface backed by in-memory
mocks. No live exchange, no real market-data provider. See module TODOs for the
governance-gated integration points (provider, auth, account data source).
"""

__version__ = "0.1.0"
